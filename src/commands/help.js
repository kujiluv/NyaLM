function help() {
  console.log(`
Commandes disponibles :
  /help   → affiche l'aide
  /clear  → vide la conversation
  /save   → sauvegarde l'historique dans un fichier
  exit    → quitter
`);
}

module.exports = help;